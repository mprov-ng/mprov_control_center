# This is for EXAMPLE ONLY!  YOU should change these settings.
# You must create a .env with these settings set properly for your deployment
# If you create a .env.custom file before you run the installer, those settings will
# be included in the generated .env file.  

# SECRET_KEY for Django.  This should be unique and generated by you, do not commit the .env to 
# your code repo.
# Use python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=''

# for development, should we enable debug?
DEBUG=1

# localhost and 127.0.0.1 are in by default, add your host's IP and/or name here.
#ALLOWED_HOSTS=myhost.domain.com,10.1.2.1

# used for session expiry.  How many seconds after last activity we will log someone out 
# of the admin interface.  Default is 1800 if not set.
SESSION_EXPIRE_SECONDS=1800 

# 
TIME_ZONE=EST

DB_ENGINE=django.db.backends.sqlite3
#DB_NAME=
#DB_USER=
#DB_PASS=
#DB_HOST=
#DB_PORT=

# default super user settings.
DJANGO_SUPERUSER_PASSWORD=admin
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_EMAIL=root@localhost

# should we use LDAP auth
LDAP_ENABLED=0

# This is the base used in all searches and templates.
LDAP_BASE="dc=example,dc=ccom"

# The LDAP URI for the server
LDAP_URI=ldaps://127.0.0.1:636

# for direct binding, use this template.
LDAP_USER_DN_TEMPLATE="uid=%(user)s,${LDAP_BASE}"

# Users must be a member of this group to have access to the UI
LDAP_ADMIN_GROUP="cn=admins,ou=Group,${LDAP_BASE}"

# set to 1 if you are using a self signed cert for SSL/TLS
LDAP_SELF_SIGNED_CERT=1

# Maps to "is_staff" flag in django
LDAP_STAFF_GROUP="cn=staff,ou=Group,${LDAP_BASE}"

# maps to "is_super" in django
LDAP_SUPER_GROUP="cn=wheel,ou=Group,${LDAP_BASE}"

# Uncomment and adjust these if you are not using direct binding.
# The dn to bind as to perform a user search for authentication
# instead of direct binding.
#LDAP_BINDDN
# The password for the above user.
#LDAP_BINDPASS
# The base search to use to find a user that you want to authenticate as.
#LDAP_USER_SEARCH


# Install Scripts Section: 
# NOTE: See each install script for any updated env vars.

# DISABLE_MPROV_SHARE disables the create_mprov_share.sh script

# NO_ANSIBLE does not install ansible at installation time

# NO_MLX_OFED does not download and install the latest MLX OFED drivers

# NO_NVIDIA does not run the nvidia sdk/driver install

# DISABLE_SLURM does not download, compile, and install the latest slurm.